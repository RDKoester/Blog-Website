<%- include("partials/header") -%>

<h1>Home</h1>

<p><%= homeContent %></p>

<% posts.forEach(function(post) { %>
    <div class="aPost">
        <h1><%= post.title %></h1>
        <p><%= post.content %></p>
    </div>
<% }) %>

<!-- <% for (i = 0; i < posts.length; i++) { %>
    <div class="aPost">
        <h2><%= posts[i].title %></h2>
        <p><%= posts[i].content %></p>
    </div>
<% } %> -->

<%- include("partials/footer") -%>